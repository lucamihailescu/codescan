# =============================================================================
# Microsoft Entra ID (Azure AD) Configuration
# =============================================================================
# Copy this file to .env and fill in your Entra ID application settings.
# These values can be found in the Azure Portal under:
# Microsoft Entra ID > App registrations > Your App
# =============================================================================

# Application (client) ID from Azure Portal
# Found in: App registrations > Your App > Overview > Application (client) ID
VITE_ENTRA_CLIENT_ID=your-client-id-here

# Directory (tenant) ID from Azure Portal
# Found in: App registrations > Your App > Overview > Directory (tenant) ID
# Use "common" for multi-tenant apps, or your specific tenant ID
VITE_ENTRA_TENANT_ID=your-tenant-id-here

# Redirect URI - must match the one registered in Azure Portal
# For local development, typically: http://localhost:5173
# For production, use your actual domain
VITE_ENTRA_REDIRECT_URI=http://localhost:5173

# Post-logout redirect URI (optional, defaults to REDIRECT_URI)
VITE_ENTRA_POST_LOGOUT_URI=http://localhost:5173

# API Scope for backend access (optional)
# Format: api://{client-id}/.default or api://{client-id}/access_as_user
# VITE_ENTRA_API_SCOPE=api://your-client-id/.default

# =============================================================================
# IMPORTANT: Azure Portal Configuration Required
# =============================================================================
# 1. Register a new application in Microsoft Entra ID
# 2. Configure Authentication:
#    - Add a Single-page application (SPA) platform
#    - Set the redirect URI to match VITE_ENTRA_REDIRECT_URI
# 3. Configure App Roles:
#    - Go to App roles > Create app role
#    - Display name: Admin
#    - Value: admin (must be lowercase)
#    - Allowed member types: Users/Groups
# 4. Assign users to the 'admin' role:
#    - Go to Enterprise applications > Your App > Users and groups
#    - Add user/group and select the 'admin' role
# 5. Configure Token:
#    - In Manifest, ensure "accessTokenAcceptedVersion": 2
#    - The roles claim will be included in the ID token
# =============================================================================
